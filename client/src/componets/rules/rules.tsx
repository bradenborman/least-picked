import React from "react";

require("./rules.scss");

export interface IRulesProps {
   
}

export const Rules: React.FC<IRulesProps> = (
  props: IRulesProps
) => {

 
  return (
      <div className="rules">
        
    </div>
  );

};